{
  "name": "evileye-frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc && copyfiles -f index.html styles/main.css ../static",
    "watch": "tsc --watch"
  },
  "devDependencies": {
    "copyfiles": "^2.4.1",
    "typescript": "^5.3.3"
  }
}
