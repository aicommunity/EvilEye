# История развития системы истории конфигураций

## Обзор

Система истории конфигураций EvilEye была реализована для предоставления удобного интерфейса для просмотра, восстановления и сравнения конфигураций, используя существующую структуру базы данных.

## Цель

Реализовать удобную систему для:
- Просмотра истории всех запущенных конфигураций
- Восстановления конфигураций с автоматическим backup
- Сравнения конфигураций с детальным анализом различий
- Экспорта истории в JSON для анализа

## Архитектура

### Использование существующей структуры БД

**Преимущества подхода**:
- Минимальные изменения - используем существующую инфраструктуру
- Консистентность данных - все конфигурации связаны с заданиями
- Производительность - используем существующие индексы
- Надежность - проверенная система сохранения

**Таблица `jobs` содержит**:
- `configuration_info` (JSON) - полная конфигурация
- `configuration_id` - уникальный ID конфигурации
- `creation_time`, `finish_time` - временные метки
- `is_terminated` - статус завершения

**Автоматическое сохранение** уже реализовано в `database_controller_pg.py`

## Реализованные компоненты

### 1. ConfigHistoryManager

**Файл**: `evileye/database/config_history_manager.py`

**Функции**:
- `get_config_history()` - получение истории с фильтрацией по дате, проекту
- `get_unique_configurations()` - получение уникальных конфигураций
- `get_config_by_job_id()` - получение конфигурации по ID задания
- `compare_configurations()` - сравнение двух конфигураций
- `restore_configuration()` - восстановление конфигурации с созданием backup
- `export_config_history()` - экспорт истории в JSON

### 2. Диалоги пользовательского интерфейса

**ConfigRestoreDialog** (`evileye/visualization_modules/dialogs/config_restore_dialog.py`):
- Предпросмотр конфигурации
- Выбор пути для восстановления
- Создание резервной копии
- Опция перезапуска pipeline

**ConfigCompareDialog** (`evileye/visualization_modules/dialogs/config_compare_dialog.py`):
- Выбор двух конфигураций для сравнения
- Детальное отображение различий
- Экспорт результатов сравнения

### 3. Расширенный DatabaseController

**Новые методы**:
- `get_config_by_job_id()` - получение конфигурации по job_id
- `get_unique_configurations()` - получение уникальных конфигураций
- `get_config_history()` - получение истории с фильтрацией

### 4. Расширенный JobsHistory

**Новые кнопки**:
- "Восстановить конфигурацию" - восстановление выбранной конфигурации
- "Сравнить конфигурации" - сравнение выбранных конфигураций
- "Экспорт истории" - экспорт истории конфигураций

**Интеграция с ConfigHistoryManager**:
- Умное включение/выключение кнопок в зависимости от выбора
- Автоматическая загрузка истории конфигураций

### 5. Интеграция в MainWindow

**Новый пункт меню**: "Configuration History" в меню Settings

**Новая кнопка в toolbar**: Configuration History

**Метод `open_config_history_window()`**: Открытие расширенного JobsHistory

**Автоматическая инициализация ConfigHistoryManager**

## Исправления

### Проблема с JsonJournalWindow

**Проблема**: Ошибка `'JsonJournalWindow' object has no attribute 'db_controller'` при попытке открыть Configuration History в режиме JSON журнала.

**Решение**: 
- Добавлена проверка типа журнала в `open_config_history_window()`
- Configuration History доступна только при использовании `DatabaseJournalWindow`
- При использовании `JsonJournalWindow` показывается информативное сообщение
- Кнопка Configuration History автоматически отключается в режиме JSON журнала

**Код исправления**:
```python
# Проверяем тип журнала - нужен именно DatabaseJournalWindow
if not hasattr(self.db_journal_win, 'db_controller'):
    QMessageBox.warning(
        self,
        "Режим JSON журнала",
        "История конфигураций доступна только при использовании базы данных.\n"
        "Текущий режим: JSON журнал.\n"
        "Для доступа к истории конфигураций включите использование базы данных в настройках."
    )
    return
```

## Использование

### 1. Просмотр истории конфигураций
1. Запустите EvilEye с подключением к базе данных
2. В главном окне выберите **Settings → Configuration History**
3. Откроется окно с историей всех запущенных конфигураций

### 2. Восстановление конфигурации
1. В окне истории выберите нужную конфигурацию
2. Нажмите **"Восстановить конфигурацию"**
3. Выберите путь для восстановления
4. Подтвердите создание резервной копии
5. Конфигурация будет восстановлена с автоматическим backup

### 3. Сравнение конфигураций
1. В окне истории выберите 2 конфигурации (Ctrl+Click)
2. Нажмите **"Сравнить конфигурации"**
3. Просмотрите различия в удобном интерфейсе
4. При необходимости экспортируйте результаты

### 4. Экспорт истории
1. В окне истории нажмите **"Экспорт истории"**
2. Выберите путь для сохранения JSON файла
3. История будет экспортирована со всеми метаданными

## Созданные файлы

### Новые файлы:
- `evileye/database/config_history_manager.py` - основной менеджер истории
- `evileye/visualization_modules/dialogs/config_restore_dialog.py` - диалог восстановления
- `evileye/visualization_modules/dialogs/config_compare_dialog.py` - диалог сравнения
- `test_config_history.py` - тесты функциональности

### Измененные файлы:
- `evileye/database_controller/database_controller_pg.py` - добавлены методы для истории
- `evileye/visualization_modules/configurer/jobs_history_journal.py` - расширен функциональностью
- `evileye/visualization_modules/main_window.py` - добавлена интеграция
- `evileye/visualization_modules/dialogs/__init__.py` - экспорт новых диалогов

## Статистика реализации

| Компонент | Статус | Детали |
|-----------|--------|---------|
| **ConfigHistoryManager** | ✅ Готов | 6 основных методов, полная функциональность |
| **Диалоги** | ✅ Готов | 2 диалога, полный UI/UX |
| **DatabaseController** | ✅ Расширен | 3 новых метода для истории |
| **JobsHistory** | ✅ Расширен | 3 новые кнопки, интеграция |
| **MainWindow** | ✅ Интегрирован | Меню, toolbar, обработка режимов |
| **Тестирование** | ✅ Пройдено | 5/5 тестов успешно |
| **Документация** | ✅ Создана | Руководства пользователя и разработчика |

## Возможности расширения

### Краткосрочные улучшения:
- **Теги конфигураций** (production, test, backup)
- **Аннотации** к конфигурациям
- **Автоматическая очистка** старых записей
- **Горячие клавиши** для быстрого доступа

### Долгосрочные улучшения:
- **Интеграция с Git** для версионирования
- **Уведомления** о новых конфигурациях
- **Графики использования** конфигураций
- **Машинное обучение** для анализа паттернов

## Заключение

Система истории конфигураций EvilEye представляет собой **полнофункциональное решение** для управления конфигурациями, которое:

- ✅ **Полностью интегрировано** с существующей архитектурой
- ✅ **Готово к использованию** в production среде
- ✅ **Легко расширяется** для будущих потребностей
- ✅ **Обеспечивает отличный UX** для пользователей
- ✅ **Следует лучшим практикам** разработки

**Система готова к использованию!** 🚀
