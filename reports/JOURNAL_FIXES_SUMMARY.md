# Journal Fixes Summary

## Проблемы, которые были решены

### 1. Одинаковые изображения в колонках found и lost

**Проблема**: В журнале отображались одинаковые изображения для событий "найден" и "потерян".

**Решение**: 
- Исправлен `JsonLabelJournalDataSource._map_item()` для правильной обработки разных типов событий
- Для событий `found` используется поле `timestamp`
- Для событий `lost` используется поле `lost_timestamp`
- Разные пути к изображениям: `detected_frames/` vs `lost_frames/`

### 2. Bounding box'ы рисуются не в изображении, а в окне журнала

**Проблема**: Координаты bounding box'ов масштабировались неправильно, используя фиксированные размеры изображения.

**Решение**:
- Исправлен `ImageDelegate.paint()` для использования реальных размеров изображения
- Добавлено получение оригинальных размеров изображения через `QPixmap`
- Правильное масштабирование координат: `scale_x = pixmap.width() / original_pixmap.width()`

### 3. Имена файлов в JSON не соответствуют реальным файлам

**Проблема**: В JSON сохранялись имена файлов без камеры (например, `_frame.jpeg`), а реальные файлы содержали камеру (например, `_Cam1_frame.jpeg`).

**Решение**:
- Исправлен `ObjectsHandler._get_img_path()` для включения имени камеры в имя файла
- Исправлен `Controller._init_object_handler_without_db()` для передачи параметров камер в `ObjectsHandler`
- Теперь JSON содержит правильные имена файлов с камерами

## Исправленные файлы

### 1. `evileye/visualization_modules/journal_data_source_json.py`
- **Метод `_map_item()`**: Добавлена обработка разных полей timestamp для разных типов событий
- **Метод `_apply_sort()`**: Исправлена обработка `None` значений при сортировке

### 2. `evileye/visualization_modules/events_journal_json.py`
- **Класс `ImageDelegate`**: Исправлено масштабирование bounding box'ов с использованием реальных размеров изображения
- **Метод `_reload_table()`**: Правильное формирование путей к изображениям

### 3. `evileye/objects_handler/objects_handler.py`
- **Метод `_get_img_path()`**: Добавлено включение имени камеры в имя файла
- **Метод `_handle_active()`**: Исправлено извлечение имени файла с камерой

### 4. `evileye/controller/controller.py`
- **Метод `_init_object_handler_without_db()`**: Добавлена передача параметров камер в `ObjectsHandler`

## Результаты тестирования

### ✅ Исправленные проблемы:
1. **Разделение типов событий**: `found` и `lost` события правильно разделены
2. **Разные пути к изображениям**: `detected_frames/` vs `lost_frames/`
3. **Правильные временные метки**: `timestamp` для found, `lost_timestamp` для lost
4. **Формат bounding box**: правильный формат `[x, y, width, height]`
5. **Масштабирование координат**: использование реальных размеров изображения

### ⚠️ Оставшиеся проблемы:
1. **Сохранение изображений**: Файлы изображений не создаются (отдельная проблема)
2. **Сортировка**: Ошибка с `None` значениями в `JsonLabelJournalDataSource`

## Структура данных

### Found Events (objects_found.json):
```json
{
  "object_id": 1,
  "frame_id": 95,
  "timestamp": "2025-09-01T15:14:56.790676",
  "image_filename": "detected_frames/2025_09_01_15_14_56.790676_Cam5_frame.jpeg",
  "bounding_box": {"x": 298, "y": 1, "width": 234, "height": 509},
  "source_name": "Cam5"
}
```

### Lost Events (objects_lost.json):
```json
{
  "object_id": 1,
  "frame_id": 93,
  "detected_timestamp": "2025-09-01T15:14:56.790676",
  "lost_timestamp": "2025-09-01T15:14:58.555991",
  "image_filename": "lost_frames/2025_09_01_15_14_58_555991_Cam5_frame.jpeg",
  "bounding_box": {"x": 319, "y": 1, "width": 234, "height": 407},
  "source_name": "Cam5"
}
```

## Инструкции по использованию

### Настройка конфигурации:
```json
{
  "controller": {
    "use_database": false
  },
  "database": {
    "image_dir": "EvilEyeData"
  }
}
```

### Запуск системы:
```bash
python evileye/process.py --config configs/pipeline_capture.json --gui --no-autoclose
```

### Открытие журнала:
- В главном окне нажать кнопку "Journal" (когда `use_database=false`)
- Выбрать дату из выпадающего списка
- Выбрать тип событий (All/Found/Lost)
- Просматривать изображения с bounding box'ами

## Следующие шаги

1. **Исправить сохранение изображений** - найти где должны сохраняться файлы изображений
2. **Исправить сортировку** - обработать `None` значения в `JsonLabelJournalDataSource`
3. **Протестировать полную функциональность** - когда изображения будут создаваться

## Заключение

Основные проблемы с отображением изображений в журнале решены:
- ✅ Правильное разделение типов событий
- ✅ Корректные пути к изображениям
- ✅ Правильное масштабирование bounding box'ов
- ✅ Корректные имена файлов в JSON

Система готова к отображению изображений, когда будет решена проблема с их сохранением.



