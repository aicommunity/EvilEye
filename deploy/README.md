# Deploy helpers

Site-facing ops tooling shipped with EvilEye (not the Python package itself).

- [`monitor/`](monitor/) — systemd user watchdog / auto-restart / incident collection

See [`monitor/README.md`](monitor/README.md) for install steps.
